name: run_tests

on:
  push:
    branches:
      - "*"

jobs:
  run_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
      - run: ./autotest.sh
